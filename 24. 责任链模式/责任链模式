责任链模式：

责任链模式，使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间的耦合关系。讲这个对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它为止。其中最关键的是当客户提交一个请求时，请求是沿着链传递直到有一个具体的处理对象处理该请求。但是需要注意一个请求可能到了链的结尾也得不到处理或因为没有正确配置而得不到处理，这个需要在实现进行全面的考虑。

这个模式的结构和状态模式有些类似：这里以一个加薪的例子实现该模式。