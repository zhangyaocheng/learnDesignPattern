迭代器模式：

迭代器模式，提供一种方法顺序访问一个聚合对象中各个元素，而又不暴露该对象的内部表示。

迭代器模式需要为遍历不同的聚集结构提供开始，下一个，是否结束，当前是那一项的接口。
当你需要遍历以及聚集的所有对象时或者对聚集对象有多个方式遍历时可以考虑使用迭代器模式。

其使用过于普遍因此被各种高级语言封装显得不太常用。在java中使用的其实也蛮多的。

这里以一个售票员买票的例子构建一个基础的迭代器模式使用方法。